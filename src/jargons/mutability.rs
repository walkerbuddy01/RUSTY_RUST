fn main() {
    let x = 32; // Everything in the rust is immutable by default;
    // x = 89;
    let name = String::from("RUSTYBUDDY");
    // name.push_str("HAYDAY");
    //  adding mute before the variable identifer will make the variable mutable
    let mut y: i8 = 32;
    y = 90;
}
